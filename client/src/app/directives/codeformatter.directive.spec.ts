import { CodeformatterDirective } from './codeformatter.directive';

describe('CodeformatterDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeformatterDirective();
    expect(directive).toBeTruthy();
  });
});
